---
name: kernel-validation
description: How to validate the Agnara kernel using this repository.
---
# Kernel Validation

This repository validates the `agnara` execution runtime. It explicitly exercises:
- `@app.capability`
- `DIRegistry` and `DIContainer`
- `ExecutionPlan` and `ExecutionContext`
- `Success` and `Failure` canonical types
